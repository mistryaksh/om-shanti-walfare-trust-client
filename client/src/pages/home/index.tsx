import React from 'react';

import { Layout } from '../../layout';
import { HomeModule } from '../../modules';

export const Home = () => {
    return (
        <Layout>
            <HomeModule />
        </Layout>
    )
}