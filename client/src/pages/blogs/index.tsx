import React, { FC } from 'react';

import { Layout } from '../../core/components/Layout';

export const Blogs: FC = () => {
   return (
      <Layout>
         <div>Blogs</div>
      </Layout>
   );
};
