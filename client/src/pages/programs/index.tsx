import React, { FC } from 'react';

import { Layout } from '../../core/components/Layout';

export const Programs: FC = () => {
   return (
      <Layout>
         <div>Programs</div>
      </Layout>
   );
};
