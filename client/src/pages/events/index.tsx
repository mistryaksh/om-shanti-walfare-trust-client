import React, { FC } from 'react';

import { Layout } from "../../core/components/Layout";

export const Events: FC = () => {
   return (
      <Layout>
         <div>Events</div>
      </Layout>
   );
};
