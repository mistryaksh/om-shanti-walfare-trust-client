import React, { FC } from 'react';

import { Layout } from '../../core/components/Layout';

export const Contact: FC = () => {
   return (
      <Layout>
         <div>Contact</div>
      </Layout>
   );
};
